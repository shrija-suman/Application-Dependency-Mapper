# Nagios configuration for Application Dependency Mapper

define host {
    use                     linux-server
    host_name               dependency-mapper
    alias                   Dependency Mapper Server
    address                 127.0.0.1
}

define service {
    use                     generic-service
    host_name               dependency-mapper
    service_description     HTTP
    check_command           check_http
}